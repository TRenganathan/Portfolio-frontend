/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "lh3.googleusercontent.com",
      "localhost",
      "portfolio-backend-4n0f.onrender.com",
    ],
  },
};

export default nextConfig;
